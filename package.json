
{
  "name": "my-vercel-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "chmod +x ./node_modules/.bin/vite || true",
    "build": "vite build",
    "dev": "vite"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
